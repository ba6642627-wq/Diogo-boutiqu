name: flutter_sales_app
version: 1.0.0

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^1.10.0
  firebase_auth: ^3.3.3
  cloud_firestore: ^3.1.0
  provider: ^6.0.0
  http: ^0.13.3
  flutter_localizations:
    sdk: flutter

flutter:
  uses-material-design: true
